export interface ActivityProps {
  title: string;
  description: string;
  icon: string;
}

export interface StageProps {
  title: string;
  description: string;
  icon: string;
}

export interface NavLinkProps {
  href: string;
  label: string;
}